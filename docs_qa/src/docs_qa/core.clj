(ns docs-qa.core
  (:require [clojure.java.jdbc :as jdbc]
            [openai-api.core :refer :all])
  (:gen-class))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))
